#include <stdio.h>

int main(void)
{
    int n;
    char name[101];
    int dd[101];
    int mm[101];
    int yyyy[101];
    int min, max;

    scanf("%d", &n);
    for (int i = 0; i < n; i++)
    {
        scanf("%s", name[i]);
        scanf("%d", dd[i]);
        scanf("%d", mm[i]);
        scanf("%d", yyyy[i]);
    }
    min = yyyy[0];
    max = yyyy[0];
    for (int i = 0; i < n; i++)
    {
        
    }
}